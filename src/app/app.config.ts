import { environment } from '../environments/environment'

export const CLIENT_ID = environment.client_id;
export const CLIENT_SECRET = environment.client_secret;
export const ROOT_API = 'https://api.foursquare.com/v2';
